# Python requirements for running this book's Jupyter notebooks locally.
# (The site itself is built with the npm `jupyter-book` CLI — see .github/workflows/deploy.yml.)

pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# For running notebooks locally
jupyterlab>=4.0.0
notebook>=7.0.0
ipywidgets>=8.0.0
